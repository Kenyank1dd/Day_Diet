<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.diet.Mapper.UserMapper">
    <select id="findAll" resultType="com.example.diet.Domain.User" >
        select * from usr
    </select>

    <select id="findFamilyAllergenbyId" resultType="java.util.Map">
        SELECT
            f.family_id,
            i.ing_name
        FROM
            usr u
                JOIN usr_family uf ON u.usr_id = uf.usr_id
                JOIN fam_allergen fa ON uf.family_id = fa.fam_id
                JOIN ingredient i ON fa.ing_id = i.ing_id
                JOIN usr_family f ON f.family_id = uf.family_id
        WHERE
            u.usr_id = #{usrId}
        ORDER BY
            f.family_id;
    </select>
    <select id="findFamilyMessagebyId" resultType="java.util.Map">
        SELECT
            f.family_id,
            f.sugar_need,
            f.cal_need,
            f.fat_need
        FROM
            usr u,
            usr_family f
        WHERE
            u.usr_id = f.usr_id
          AND u.usr_id = #{usrId}
        ORDER BY
            f.family_id
    </select>

    <select id="GetUser" parameterType="String" resultType="com.example.diet.Domain.User">
        select * from usr where usr_phone = #{user_phone}
    </select>
    <insert id="InsertUser" parameterType="com.example.diet.Domain.User">
        insert into usr values (null, #{usr_name},#{usr_phone}, null, #{usr_password},
                                #{usr_sex},#{usr_age}, #{usr_height},#{usr_weight},#{usr_weight},#{tar_weight},#{tar_time},
                                null,#{reg_time})
    </insert>
    <select id="RecordWater" parameterType="int" resultType="Integer">
        select water_num from water where usr_id = #{userId}
    </select>
    <select id="GetDayWater" parameterType="int" resultType="String">
        select water_date from water where usr_id = #{userId}
    </select>
    <update id="SetZero" parameterType="int" >
        update water
        <set>
            water_num = 0
        </set>
        where usr_id = #{userId}
    </update>
    <update id="UpdateDayWater">
        update water
        <set>
            water_date = #{today}
        </set>
        where usr_id = #{userId}
    </update>
    <update id="UpdateWater">
        update water
        <set>
            water_num = water_num + #{water_num}
        </set>
        where usr_id = #{userId}
    </update>


    <update id="updateInfo">
        UPDATE usr
        SET usr_name = #{usr_name},
            usr_sex = #{usr_sex},
            usr_height = #{usr_height},
            new_weight = #{new_weight},
            tar_weight = #{tar_weight},
            tar_time = #{tar_time}
        WHERE
            usr_id = #{usr_id}
    </update>
    <update id="UpdateWeight">
        UPDATE weight
        SET
            usr_weight = #{weight}
        WHERE
            usr_id = #{userId}
            AND date = #{time}
    </update>
    <update id="UpdateFamily">
        update familyinfo
        set disease = #{disease} ,
            allergen = #{allergen},
            sugar_need = #{sugar_need},
            cal_need = #{cal_need},
            fat_need = #{fat_need}
        where usr_id = #{usr_id} AND relation = #{relation}
    </update>
    <update id="updateSport">
        UPDATE sport
        SET consume = #{sport}
           And step = #{step}
        WHERE
            usr_id = #{userId}
          AND date = #{date}
    </update>

    <select id="GetFamily" parameterType="int" resultType="UsrFamily">
        SELECT
            *
        FROM
            usr_family
        WHERE
            usr_id1 = #{userId}
           OR usr_id2 = #{userId}
    </select>

    <select id="GetBreakDiet" parameterType="int" resultType="Recent_diet">
        select * from recent_diet where rd_usr = #{userId} and rd_type = 1
    </select>

    <select id="GetLunchDiet" parameterType="int" resultType="Recent_diet">
        select * from recent_diet where rd_usr = #{userId} and rd_type = 2
    </select>

    <select id="GetDinnerDiet" parameterType="int" resultType="Recent_diet">
        select * from recent_diet where rd_usr = #{userId} and rd_type = 3
    </select>

    <select id="GetRecipe" parameterType="long" resultType="Recipe">
        select * from recipe where rec_id = #{recipe_id}
    </select>

    <select id="GetWeight" parameterType="int" resultType="com.example.diet.Domain.WeightRecord">
        select * from weight where usr_id = #{userId} order by date
    </select>

    <insert id="InsertRegisterInfo">
        insert into registerinfo values (#{usr_phone},#{usr_password},#{usr_name},#{usr_sex},
                                         #{usr_height},#{tar_weight},#{usr_age},#{tar_weight},
                                         #{tar_time})
    </insert>
    <insert id="InsertWeight">
        insert into weight values (#{userId},#{weight},#{time})
    </insert>
    <insert id="InsertFamily">
        insert into familyinfo values (null, #{usr_id},#{relation},#{disease},#{allergen},#{sugar_need},#{cal_need},#{fat_need})
    </insert>
    <insert id="InsertDiet">
        insert into recent_diet values (null , #{rd_usr},#{rd_rec},#{rd_time},#{rd_type})
    </insert>
    <insert id="InsertWater">
        insert into water values (#{userId},#{i},#{date})
    </insert>
    <insert id="InsertCal">
        insert into cal values (#{userId},#{i},#{date})
    </insert>
    <insert id="InsertFamilyRelation">
        insert into usr_family values (#{usr_phone1},#{usr_phone2},#{relation1},#{relation2})
    </insert>
    <insert id="InsertSport">
        insert into sport values (#{userId},#{sport},#{step},#{date})
    </insert>

    <select id="findUserbyPhonenum" resultType="com.example.diet.Domain.User">
        SELECT
            *
        FROM
            usr
        WHERE
            usr_phone = #{userphone}
          AND usr_password = #{password}
    </select>
    <select id="findUserbyPhonenum1" resultType="com.example.diet.Domain.User">
        SELECT
            *
        FROM
            usr
        WHERE
            usr_phone = #{userphone}
    </select>
    <select id="findById" resultType="com.example.diet.Domain.User">
        SELECT *
        FROM usr
        WHERE usr_id = #{usrId}
    </select>
    <select id="findUserbyDateId" resultType="com.example.diet.Domain.WeightRecord">
        SELECT
            *
        FROM
            weight
        WHERE
            usr_id = #{userId}
          AND date = #{time}
    </select>
    <select id="findFamilyByusrId" resultType="com.example.diet.Domain.FamilyInfo">
        SELECT
            *
        FROM
            familyinfo
        WHERE
            usr_id = #{usrId}
    </select>
    <select id="Get_dis_all" resultType="com.example.diet.Domain.FamilyInfo">
        select * from familyinfo where usr_id = #{userId}
    </select>
    <select id="GetFamilyInfo" resultType="com.example.diet.Domain.FamilyInfo">
        select * from familyinfo where usr_id = #{userId} AND relation = #{relation}
    </select>
    <select id="GetRecipeId" resultType="java.lang.Long">
        select rec_id from recipe where rec_name = #{rec_name}
    </select>

    <select id="getUserId" resultType="java.lang.Long">
        select usr_id from usr where usr_phone = #{usrPhone}
    </select>
    <select id="findAllergenById" resultType="java.lang.String">
        SELECT
            i.ing_name
        FROM
            usr_allergen a,
            ingredient i
        WHERE
            a.ing_id = i.ing_id
          AND a.usr_id = #{usr_id}
    </select>
    <select id="findDiseaseById" resultType="java.lang.String">
        SELECT
            di.dis_name
        FROM
            usr_dis d,
            disease di
        WHERE
            d.dis_id = di.dis_id
          AND d.usr_id = #{usr_id}
    </select>
    <select id="findUserById" resultType="com.example.diet.Domain.User">
        SELECT
            *
        FROM
            usr
        WHERE
            usr_id = #{usr_id}
    </select>
    <select id="getSexByUserId" resultType="java.lang.Integer">
        SELECT
            usr_sex
        FROM
            usr
        WHERE
            usr_phone = #{usr_id}
    </select>
    <select id="findTasteIdById" resultType="java.lang.Integer">
        SELECT
            ut.taste_id
        FROM
            usr u,
            usr_taste ut
        WHERE
            u.usr_id = ut.usr_id
          AND u.usr_id = #{usr_id};
    </select>
    <select id="findLabelIdById" resultType="java.lang.Integer">
        SELECT
            ul.label_id
        FROM
            usr u,
            usr_label ul
        WHERE
            u.usr_id = ul.usr_id
          AND u.usr_id = #{usr_id};
    </select>
    <select id="findSportByIdDate" resultType="java.lang.Integer">
        SELECT
            consume
        FROM
            sport
        WHERE
            usr_id = #{userId}
          AND date = #{date}
    </select>

</mapper>

